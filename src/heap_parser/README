Those files are a parser able to analyze a dump given by the HeapInspector
program (see sawja/jvmti/HeapInspector). It is used to recover an ocaml
representation of the heap of a java program.

They are compiled through the usual sawja compilation system.

Exemple or used are available in jRTAWithHeap and Nit analysis.
